[package]
name = "list-target-features"
version = "0.1.0"
edition = "2021"
publish = false

[dependencies]
askama = { version = "0.16", default-features = false, features = ["config", "derive", "std"] }

[workspace]
